---
description: React 组件与 hooks 规范（单一职责、抽 hooks、减少嵌套/分支）
globs: **/*.tsx, **/*.jsx
alwaysApply: false
---

## 组件（别把组件写成脚本）

- 单文件一个导出组件；命名清晰、职责单一
- 复杂逻辑抽 `useXxx`，组件只负责渲染与组合
- 避免深层嵌套（UI 用组合/拆组件，逻辑用 hooks/纯函数）

## Hooks（铁律）

- hooks 只在顶层调用：不进循环、不进条件、不进嵌套函数
- 自定义 hook 必须以 `use` 开头，且只做一件事

## 状态（数据放对地方）

- 组件私有状态就近放；只有被多个不相关区域共享才上全局
- “服务端状态”别塞全局 store：优先交给数据请求层（Query/SWR 等）
