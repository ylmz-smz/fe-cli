---
description: TypeScript 代码底线（类型/错误处理/简洁，避免“补丁式 if”）
globs: **/*.ts, **/*.tsx
alwaysApply: false
---

## 错误处理（别吞错）

- 不允许空 `catch`、不允许“只 log 不 throw/return”
- 抛错要保留 cause（优先 `new Error(msg, { cause })`，或包装后透传）

## 类型（少搞花活）

- 数据结构先行：优先把“可空/可缺省/状态机”建模出来，而不是到处 `if (x)`
- 避免 `any`；必要时用 `unknown` + 显式收窄
- 能用 **联合类型** 表达状态就别用多个布尔开关

## 复杂度（把分支变少）

- 看到一串 `if/else`：先问“是不是数据结构错了”
- 一个函数只做一件事；超过 30-40 行优先拆
