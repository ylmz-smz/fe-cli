---
description: CLI 生成器代码规范（src/generators）：数据流清晰、最小副作用、向后兼容
globs: src/generators/**/*.ts
alwaysApply: false
---

## 核心原则

- **Never break userspace**：输出目录结构/文件名/默认行为尽量兼容旧项目
- 生成器是“纯数据 → 文件”的流水线：先构建 data，再一次性写出

## 结构与复杂度

- 把“选项解析/数据建模/文件写入”拆开：别揉成一个 200 行函数
- 通过数据结构消除分支：例如用 map/表驱动替代一串 `if (framework === ...)`

## 可测试性（别把 IO 写死）

- IO 边界集中在 1-2 个函数；其余逻辑保持纯函数，方便单测
